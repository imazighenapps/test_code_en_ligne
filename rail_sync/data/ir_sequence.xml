<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Sequences for config files -->
        <record id="ir_sequence_config_file" model="ir.sequence">
            <field name="name">Sequence config file</field>
            <field name="code">ir.sequence.config.file</field>
            <field name="prefix">%(year)s/</field>
            <field name="padding">3</field>
            <field name="company_id" eval="False"/>
        </record>

    </data>
</odoo>
